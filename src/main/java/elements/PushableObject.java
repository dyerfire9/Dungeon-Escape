package elements;

public class PushableObject extends Element implements Movable {
    public PushableObject(String sprite, int[] pos){
        super(sprite, pos);
    }

    @Override
    public void move() {
    }
}
